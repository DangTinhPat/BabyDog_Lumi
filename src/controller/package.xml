<?xml version="1.0"?>
<?xml-model href="http://download.ros.org/schema/package_format3.xsd" schematypens="http://www.w3.org/2001/XMLSchema"?>
<package format="3">
    <name>controller</name>
    <version>0.0.0</version>
    <description>ros2_control controller: stand up / sit down FSM (no gait, no active balance yet) + FK/IK (KDL, ported from superDog's unitree_guide_controller) used for static Tff (gravity feedforward, sim only, off by default)</description>
    <maintainer email="dangtinh.ftcpy@gmail.com">dvt</maintainer>
    <license>Apache-2.0</license>

    <buildtool_depend>ament_cmake</buildtool_depend>
    <buildtool_depend>rosidl_default_generators</buildtool_depend>

    <depend>controller_interface</depend>
    <depend>pluginlib</depend>
    <depend>rcpputils</depend>
    <depend>realtime_tools</depend>
    <depend>rclcpp</depend>
    <depend>hardware_interface</depend>
    <depend>std_msgs</depend>
    <depend>kdl_parser</depend>

    <exec_depend>controller_manager</exec_depend>
    <exec_depend>joint_state_broadcaster</exec_depend>
    <exec_depend>rosidl_default_runtime</exec_depend>

    <member_of_group>rosidl_interface_packages</member_of_group>

    <test_depend>ament_lint_auto</test_depend>
    <test_depend>ament_lint_common</test_depend>

    <export>
        <build_type>ament_cmake</build_type>
    </export>
</package>
